name: students_grades
description: تطبيق لإدارة درجات الطلاب وحساب المجموع والنسبة
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"
  # flutter: ">=3.0.0"   # يمكن تركها معلقة أو تحذفها

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15  # نسخة أقدم متوافقة مع Dart 2.19.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
